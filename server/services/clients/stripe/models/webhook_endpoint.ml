(*
 * This file has been generated by the OCamlClientCodegen generator for openapi-generator.
 *
 * Generated by: https://openapi-generator.tech
 *
 * Schema Webhook_endpoint.t : You can configure [webhook endpoints](https://stripe.com/docs/webhooks/) via the API to be notified about events that happen in your Stripe account or connected accounts.  Most users configure webhooks from [the dashboard](https://dashboard.stripe.com/webhooks), which provides a user interface for registering and testing your webhook endpoints.  Related guide: [Setting up Webhooks](https://stripe.com/docs/webhooks/configure).
 *)

type t = {
  (* The API version events are rendered as for this webhook endpoint. *)
  api_version : string option; [@default None]
  (* The ID of the associated Connect application. *)
  application : string option; [@default None]
  (* Time at which the object was created. Measured in seconds since the Unix epoch. *)
  created : int32;
  (* An optional description of what the webhook is used for. *)
  description : string option; [@default None]
  (* The list of events to enable for this endpoint. `['*']` indicates that all events are enabled, except those that require explicit selection. *)
  enabled_events : string list;
  (* Unique identifier for the object. *)
  id : string;
  (* Has the value `true` if the object exists in live mode or the value `false` if the object exists in test mode. *)
  livemode : bool;
  (* Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. *)
  metadata : (string * string) list;
  (* String representing the object's type. Objects of the same type share the same value. *)
  _object : Enums.deleted_webhook_endpoint_object; [@default `Webhook_endpoint]
  (* The endpoint's secret, used to generate [webhook signatures](https://stripe.com/docs/webhooks/signatures). Only returned at creation. *)
  secret : string option; [@default None]
  (* The status of the webhook. It can be `enabled` or `disabled`. *)
  status : string;
  (* The URL of the webhook endpoint. *)
  url : string;
}
[@@deriving yojson { strict = false }, show]

(** You can configure [webhook endpoints](https://stripe.com/docs/webhooks/) via the API to be notified about events that happen in your Stripe account or connected accounts.  Most users configure webhooks from [the dashboard](https://dashboard.stripe.com/webhooks), which provides a user interface for registering and testing your webhook endpoints.  Related guide: [Setting up Webhooks](https://stripe.com/docs/webhooks/configure). *)
let create (created : int32) (enabled_events : string list) (id : string)
    (livemode : bool) (metadata : (string * string) list)
    (_object : Enums.deleted_webhook_endpoint_object) (status : string)
    (url : string) : t =
  {
    api_version = None;
    application = None;
    created;
    description = None;
    enabled_events;
    id;
    livemode;
    metadata;
    _object;
    secret = None;
    status;
    url;
  }
